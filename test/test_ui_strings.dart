/// Strings mirrored from UI for stable widget tests. When adding ARB, replace
/// these with generated accessors or import a shared `test_l10n` shim.
abstract final class TestUiStrings {
  static const emptyIntercambiosTitle = 'Sin cromos para intercambio';
}
